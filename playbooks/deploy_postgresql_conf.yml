---
- name: "Déploiement de la configuration postgresql"
  hosts: database
  become: true
  tasks:
    - name: Exécution du rôle postgresql
      ansible.builtin.include_role:
        name: postgresql
        tasks_from: conf_manager.yml

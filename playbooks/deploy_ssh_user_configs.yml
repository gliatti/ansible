---
- name: "Déploiement des configurations ssh utilisateur"
  hosts: "{{ target_hosts | default([]) }}"
  become: true
  tasks:
    - name: Exécution du rôle ssh
      ansible.builtin.include_role:
        name: ssh
        tasks_from: user_config_manager_wrapper

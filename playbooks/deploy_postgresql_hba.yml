---
- name: "Déploiement du pg_hba postgresql"
  hosts: database
  become: true
  tasks:
    - name: Exécution du rôle postgresql
      ansible.builtin.include_role:
        name: postgresql
        tasks_from: hba_manager.yml
